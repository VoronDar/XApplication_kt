package com.astery.xapplication.repository.localDataStorage.dao

import androidx.room.Dao

@Dao
interface DesireDao {

    /*
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun addConsequence(eventTemplate: EventTemplate?)

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun addConsequences(eventTemplates: List<EventTemplate?>?)
    */

    /*
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    void addDesire(Desire desire);
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    void addDesires(List<Desire> desires);
    
     */

    /*
    @Update(onConflict = OnConflictStrategy.REPLACE)
    suspend fun updateConsequence(eventTemplate: EventTemplate?)

    @Update(onConflict = OnConflictStrategy.REPLACE)
    suspend fun updateConsequences(eventTemplates: List<EventTemplate?>?)
     */

    /*
    @Update(onConflict = OnConflictStrategy.REPLACE)
    void updateDesire(Desire desire);
    @Update(onConflict = OnConflictStrategy.REPLACE)
    void updateDesires(List<Desire> desires);
    
     */
}